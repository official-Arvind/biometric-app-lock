name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      mirror_tag: ${{ steps.mirror_tag.outputs.mirror_tag }}

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Derive mirror tag
        id: mirror_tag
        run: |
          code=$(grep '^version\.code=' gradle.properties | cut -d= -f2)
          name=$(grep '^version\.name=' gradle.properties | cut -d= -f2)
          echo "mirror_tag=${code}-${name}" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Decode keystore
        env:
          RELEASE_KEYSTORE: ${{ secrets.RELEASE_KEYSTORE }}
        run: |
          if [ -n "$RELEASE_KEYSTORE" ]; then
            echo "$RELEASE_KEYSTORE" | base64 -d > ${{ runner.temp }}/release.keystore
          fi

      - name: Build APKs
        env:
          RELEASE_KEYSTORE: ${{ secrets.RELEASE_KEYSTORE }}
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: |
          chmod +x gradlew
          if [ -n "$RELEASE_KEYSTORE" ]; then
            export RELEASE_STORE_FILE=${{ runner.temp }}/release.keystore
          fi
          ./gradlew :app:assembleRelease :app:assembleDebug

      - name: Rename APKs
        run: |
          # Sanitize branch/tag name (replaces / with -)
          SAFE_REF=$(echo "${{ github.ref_name }}" | tr '/' '-')
          
          # Dynamically find and rename the release APK
          for file in app/build/outputs/apk/release/*.apk; do
            mv "$file" "app/build/outputs/apk/release/biometric-app-lock-${SAFE_REF}-release.apk"
          done
          
          # Dynamically find and rename the debug APK
          for file in app/build/outputs/apk/debug/*.apk; do
            mv "$file" "app/build/outputs/apk/debug/biometric-app-lock-${SAFE_REF}-debug.apk"
          done

      - name: Generate changelog
        id: cliff
        uses: orhun/git-cliff-action@v4
        with:
          config: .github/cliff.toml
          args: --latest --strip header
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create release
        id: gh_release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_type == 'tag' && github.ref_name || steps.mirror_tag.outputs.mirror_tag }}
          files: |
            app/build/outputs/apk/release/*.apk
            app/build/outputs/apk/debug/*.apk
          body: ${{ steps.cliff.outputs.content }}

      - name: Upload APK artifacts for mirror job
        uses: actions/upload-artifact@v7
        with:
          name: release-apk
          path: |
            app/build/outputs/apk/release/*.apk
            app/build/outputs/apk/debug/*.apk
          retention-days: 1

  mirror:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
    env:
      TAG_NAME: ${{ github.ref_type == 'tag' && github.ref_name || needs.build.outputs.mirror_tag }}
      MIRROR_TAG: ${{ needs.build.outputs.mirror_tag }}

    steps:
      - name: Download APK artifact
        uses: actions/download-artifact@v8
        with:
          name: release-apk
          path: ./artifacts

      - name: Fetch release info
        id: release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          NAME=$(gh release view "$TAG_NAME" --repo "$REPO" --json name -q ".name")
          PRERELEASE=$(gh release view "$TAG_NAME" --repo "$REPO" --json isPrerelease -q ".isPrerelease")
          echo "name=$NAME" >> "$GITHUB_OUTPUT"
          echo "prerelease=$PRERELEASE" >> "$GITHUB_OUTPUT"

          echo "body<<BODY_EOF" >> "$GITHUB_OUTPUT"
          gh release view "$TAG_NAME" --repo "$REPO" --json body -q ".body" >> "$GITHUB_OUTPUT"
          echo "BODY_EOF" >> "$GITHUB_OUTPUT"

      - name: Create mirror release
        uses: softprops/action-gh-release@v3
        with:
          repository: Xposed-Modules-Repo/eu.hxreborn.biometricapplock
          token: ${{ secrets.XPOSED_REPO_PAT }}
          tag_name: ${{ env.MIRROR_TAG }}
          name: ${{ steps.release.outputs.name }}
          body: ${{ steps.release.outputs.body }}
          prerelease: ${{ steps.release.outputs.prerelease }}
          files: ./artifacts/**/biometric-app-lock-*.apk
          fail_on_unmatched_files: true
          make_latest: true
